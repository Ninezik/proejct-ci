[project]
name = "project_ci"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "pandas>=2.3.3",
]

[tool.uv]
dev-dependencies = [
  "pytest",
  "ruff"
]

[tool.ruff]
line-length = 88
